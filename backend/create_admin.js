const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcrypt'); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash('admin123456', 10); const user = await prisma.users.create({ data: { phone: '0888888888', password_hash: hash, full_name: 'Super Admin', role: 'ADMIN', status: 'ACTIVE' } }); await prisma.admins.create({ data: { admin_id: user.user_id, department: 'Ban Giám Ð?c', access_level: 'SUPER_ADMIN' } }); console.log('Admin created successfully!', user); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
